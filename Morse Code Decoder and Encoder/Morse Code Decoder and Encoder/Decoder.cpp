#include "Decoder.h"
Decoder::Decoder() {
	//Default constructor, initialize empty tree

}

Binary_Tree<string>Decoder::build_tree() {
	//Reads morse.txt and builds tree based on frequency of each letter

}

string Decoder::decode(string in) {
	//Passed in a string containing morse code, decodes into letters

}
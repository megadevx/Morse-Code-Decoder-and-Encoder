#include "MorseTree.h"

template<typename Item_Type>
MorseTree::parse(string in) {
	//Reads in morse.txt and builds BT/hash table
	string letter = "";
	string symbols = "";
	ifstream morse.open("morse.txt");
	while (morse.good()) {
		//Read the letter
		//Read the symbols
		//Build tree according to example
	}
}
